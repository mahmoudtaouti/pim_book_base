import 'package:flutter/cupertino.dart';

class NoteContentScreen extends StatelessWidget {
  const NoteContentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
